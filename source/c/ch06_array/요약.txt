배열에는 값 또는 주소들이 들어감
배열(array)은 동일 자료형이 집합, 다른언어의 경우 동일 자료형이 아닐 수도 있음'

2차원 배열의 경우 실무 사용 x
사실 2차원 배열의 경우 인접해있지 않음 (C언어의 경우)

배열 또한 한번에 초기화 가능
배열은 반드시 인접하게 만들어 놈

배열의 크기를 생략하지 않으면 그 방 갯구만큼 메모리 잡힘, 일부만 초기화해도 나머지 공간에는 0으로 초기화

문자열은 그냥 문자 배열이다.

배열에는 값 또는 주소들이 들어감
배열(array)은 동일 자료형이 집합, 다른언어의 경우 동일 자료형이 아닐 수도 있음'

2차원 배열의 경우 실무 사용 x
사실 2차원 배열의 경우 인접해있지 않음 (C언어의 경우)

배열 또한 한번에 초기화 가능

NULL문자 없으면 뒤로 계속 받게 됨(끝마침)


이름(빈문자 포함), 주소(빈문자포함)를 입력받기
scanf("%s",변수,입력받을byte수) 치명적 단점 존재 white space  (화면상에 하얀색 문자로 보이는 것 enter or space or "\n") 그 이전 버퍼내용을 return함
, 단 처음 나오는 white space는 무시


두개의 scanf를 받으려고 할 떄 white space가 존재하면 그앞의 값을 리턴하고 버퍼에 남아있는 \n아닌 값을 다음 scanf에 받게 된다.
->gets(변수)라는 함수 많이 사용
'\n'이전까지만 인식하고 그 뒤는 버퍼에서 지워버림 앞에서 scanf를 사용해서 \n이 남아있으면 \n의 빈 스트림만 들어가게 됨. (유의)
버퍼의 상태를 고려해서 코드를 짜야함.
버퍼 지우기 위해 gets함수를 한번 더 이용하는 테크닉이 있음.

문자열은 포인터(주소)에 들어있다.
문자열의 경우 주소를 str1이 가리키는 주소값을 넣게 된다.
문자열은 새로운 값을 넣게 되면, 기존에 있는 주소를 버리고 새로운 주소로 할당하게 됨. (문자열만 해당하는 내용)