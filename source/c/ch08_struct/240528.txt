여러 자료형이 묶여 만든 새로운 타입
구조체는 타입을 만든다고 보면 됨

구조체 배열도 가능함

구조체 : 여러 형태의 데이터를 하나의 단일 자료형으로 

초기화도 바로 가능, 하지만 멤버 선언순서로 해야함
={num, grade}

scanf로 받을 떄는 배열 선언시 char* name 형식 유의

구조체 배열은 n개의 공간에 한 공간마다 멤버들로 이루어져있음

type of struct vision Vision;//struct vision을 Vision으로 타입으로 

한번에 초기화도 가능함

배열 가능 

함수 가능

구조체는 그저 타입이므로 다른것과 같이 사용할 수 있다.

배열의 경우 치명적인 단점
1. 미리 만들어논 데이터 용량만큼의 낭비
2. 데이터가 그 이상 들어오면 처리가 불가

//링크드리스트 
//앞에는 주소 뒤에는 주소 인 구조체를 만들어야함.
//구조체 포인터는 이때 사용한다.

